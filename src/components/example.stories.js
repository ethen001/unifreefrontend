import React from 'react';
import { Message } from './example';

// export the title of the Component as default story
export default { title: 'Message' };

// story for the component
export const withText = () => <Message />;
